%h1 Listing courses

%table
  %thead
    %tr
      %th Course number
      %th Short title
      %th Description
      %th Meet days
      %th Semester
      %th
      %th
      %th
      
  %tbody
    - @courses.each do |course|
      %tr
        %td= course.course_number
        %td= course.short_title
        %td= course.description
        %td= course.meet_days
        %td= course.semester.short_title
        %td= link_to 'Show', course
        %td= link_to 'Edit', edit_course_path(course)
        %td= link_to 'Destroy', course, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Course', new_course_path
